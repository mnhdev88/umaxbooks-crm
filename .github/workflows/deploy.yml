name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            sudo git config --global --add safe.directory /var/www/umaxbooks-crm
            sudo chown -R ubuntu:ubuntu /var/www/umaxbooks-crm
            cd /var/www/umaxbooks-crm
            git pull origin main
            npm install --legacy-peer-deps
            npm run build
            sudo pm2 restart umaxbooks || sudo pm2 start npm --name umaxbooks -- start
